# HaloCueProject model

The canonical project model will be implemented here. It owns stable IDs,
storyGraph semantics, characters, assets, variables, AA/MMT presentation data,
AI configuration, and save state. It must remain independent of React, Tauri,
PixiJS, Unity, or private game formats.
