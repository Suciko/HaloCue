{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Suciko/HaloCue/contracts/preview-intent/1.1.schema.json",
  "title": "HaloCue editor preview intent",
  "description": "A resolved editor selection or explicit playhead and deterministic preview frame.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "scene_id",
    "cue_id",
    "selection_kind",
    "selected_event_id",
    "target"
  ],
  "properties": {
    "schema_version": { "const": "preview-intent/1.1" },
    "scene_id": { "type": "string", "minLength": 1, "maxLength": 512 },
    "cue_id": { "type": "string", "minLength": 1, "maxLength": 512 },
    "selection_kind": { "enum": ["cue", "event", "playhead"] },
    "selected_event_id": {
      "type": ["string", "null"],
      "minLength": 1,
      "maxLength": 512
    },
    "target": {
      "type": "object",
      "additionalProperties": false,
      "required": ["event_id", "frame", "alignment", "resolution"],
      "properties": {
        "event_id": { "type": "string", "minLength": 1, "maxLength": 512 },
        "frame": { "type": "integer", "minimum": 0 },
        "alignment": { "enum": ["start", "end", "exact"] },
        "resolution": {
          "enum": [
            "selected-event",
            "cue-terminal",
            "prior-renderable",
            "scene-start",
            "explicit-frame"
          ]
        }
      }
    }
  },
  "allOf": [
    {
      "if": {
        "properties": { "selection_kind": { "enum": ["cue", "playhead"] } },
        "required": ["selection_kind"]
      },
      "then": { "properties": { "selected_event_id": { "type": "null" } } }
    },
    {
      "if": {
        "properties": { "selection_kind": { "const": "event" } },
        "required": ["selection_kind"]
      },
      "then": { "properties": { "selected_event_id": { "type": "string" } } }
    },
    {
      "if": {
        "properties": {
          "target": {
            "properties": {
              "resolution": { "enum": ["selected-event", "scene-start"] }
            },
            "required": ["resolution"]
          }
        },
        "required": ["target"]
      },
      "then": {
        "properties": {
          "target": { "properties": { "alignment": { "const": "start" } } }
        }
      }
    },
    {
      "if": {
        "properties": {
          "target": {
            "properties": {
              "resolution": { "enum": ["cue-terminal", "prior-renderable"] }
            },
            "required": ["resolution"]
          }
        },
        "required": ["target"]
      },
      "then": {
        "properties": {
          "target": { "properties": { "alignment": { "const": "end" } } }
        }
      }
    },
    {
      "if": {
        "properties": { "selection_kind": { "const": "playhead" } },
        "required": ["selection_kind"]
      },
      "then": {
        "properties": {
          "target": {
            "properties": {
              "alignment": { "const": "exact" },
              "resolution": { "const": "explicit-frame" }
            }
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "target": {
            "properties": { "resolution": { "const": "explicit-frame" } },
            "required": ["resolution"]
          }
        },
        "required": ["target"]
      },
      "then": {
        "properties": { "selection_kind": { "const": "playhead" } }
      }
    }
  ]
}
