# AA 自动写剧本：从这里开始

这份说明写给不熟悉命令行和 Python 的使用者。平时不需要进入程序文件夹，也不需要输入命令。

## 最短使用方法

1. 打开总目录 `AA自动写剧本文件`。
2. 双击 `启动AA自动写剧本.cmd`。
3. 等待网页自动打开。
4. 在“剧情工作台”按照页面中的 1～4 步操作。
5. 生成完成后打开 AzureArchive，在工程列表里找到刚才填写的工程名称。

黑色窗口是本地程序本身。使用期间请保留它；关闭窗口即可停止程序。

## 第一次启动

启动器会自动检查：

- Python 版本是否可用。
- 程序文件是否完整。
- 素材数据库是否存在。
- AzureArchive 工作区是否能找到。

如果没有自动找到 AA，会弹出文件夹选择窗口。以下两种目录任选一种：

- `……\存储文件\data`，里面能看到 `projects` 文件夹。
- `……\存储文件`，它的下一层是 `data`。

选择成功后会被记住，以后不需要重复选择。

网页顶部的首次使用卡片会显示三项状态：

- **AA 工作区**：必须显示“已连接”。
- **素材数据库**：必须显示“可用”。
- **AI 模型**：只有使用“AI 自动安排演出”时才需要；“仅转换格式”无需配置。

## 生成一章剧情

### 1. 选择剧情文本

点击“选择文件”，选择 `.txt` 或 `.md` 剧情文件，再点击“读取并初审”。程序只读取文件，不会修改原文。读取时会依次显示：建立本章工作区、识别写作格式、提取规则线索、AI 通读全文。AI 需要一点时间时，进度条会停在“AI 通读全文并核对素材”，不是卡死。

### 2. 确认 AI 初审

初审会把剧本全文和**当前章节已登记的自定义素材**一起检查，列出：

- 说话或出场角色，以及建议对应的 AA 角色。
- 哪些角色需要自定义骨骼。
- 剧本提到的背景、音效和 BGM；缺的素材会明确标成“待补”。
- 指令格式、角色映射和素材引用中的问题。

先在这里处理红色问题。角色可以点“修改”；背景和音效可从本地导入，或从历史项目复制。确认初审后才会进入后续生成步骤，避免一直到编译才发现素材缺失。

### 3. 确认演员与开场背景

检查每个名字是否对应正确角色和立绘：

- 有立绘的角色选择对应人物。
- 旁白选择“旁白”。
- 老师、系统提示或只说话不显示立绘的人物可以选择“无立绘”。

搜索并选择故事开场使用的背景。这里和“本剧情素材”只显示本章已经登记的自定义素材；AA 自带的大型素材库不会铺在页面上。

### 4. 确认生成方式

- **AI 自动安排演出**：AI 添加表情、动作、气泡、音效和镜头标注，需要先在“设置”中配置模型。
- **仅转换格式**：不调用 AI，直接把已有标注转换成 AA 工程。

填写“AA 工程名称”，通常保持“生成完成后直接安装到 AA”勾选，然后点击生成。

## 推荐剧本写法

任何 `.txt` / `.md` 都可以导入。推荐使用下面这种结构，规则提取会更快、更稳定：

```text
## 放学后的天台
@bg 雨夜天台
@bgm 123

凯伊：今天也辛苦了。
旁白：远处传来雨声。
@se 敲门
爱丽丝(03)[惊讶]：有人来了？
```

- 一句台词一行，使用 `角色名：台词`（中文或英文冒号都可以）。
- 用 `## 场景标题` 分隔场景；背景用 `@bg 背景名`，音效用 `@se 音效名`。
- 自定义背景、音效、骨骼必须先复制或导入到**当前这一章**，再让初审确认。
- BGM 暂时只支持已经确认的数字 ID；自定义 BGM 的 AA 登记规则还在验证中。

如果你习惯用小说体、分镜表、角色名不在行首或混合 Markdown，也可以直接导入。页面会显示“自由文本／非标准格式”提示，并让 AI 全文提取角色、骨骼、背景和音效。请逐项确认 AI 新发现的内容，再继续生成。

## 自定义素材：本章使用与跨章整理

AA 的每个工程都要保存自己的素材副本。不要把“素材工作台”理解成全局素材链接：它只用来整理素材属于哪个系列、已经复制到哪些章节。

- **本剧情素材**：当前这一章实际可用、会交给 AI 初审和编译的素材。要用历史素材时，在这里点“从历史导入”，它会复制并登记到本章。
- **素材工作台**：右上角入口。可把骨骼、背景、音效标成“系列共用”或“章节专用”，填写系列名，查看已登记到哪些章节。这里不复制、不安装，也不会让其他章节自动获得素材。
- **骨骼表情**：导入角色骨骼后会单独进行表情差分与语义标注；它不占用剧本生成步骤。若没有配置 Spine 3.8，素材仍可导入，但会标记为“表情待检测”。

## 导入自定义素材

打开一章剧情后，在“本剧情素材”中选择背景、音效或人物。推荐把文件分别放进当前章节的素材文件夹：`bgs`、`sounds`、`characters`，然后点击“扫描素材目录”批量登记；也可以“从本地导入”单个素材，或“从历史导入”复制以前章节的素材。

### 背景

- 选择一张 PNG、JPG 或 JPEG 图片。
- 建议使用横向 16:9。
- 中文文件名可以使用。

### 音效

- 优先使用 PCM16 WAV。
- 中文文件名可以使用。
- 命令特效本身不会自动附带音效，需要单独选择真实存在的音效。

### 人物骨骼

- 选择同时包含 `.skel`、`.atlas` 和纹理 `.png` 的目录。
- `Identifier` 必须填写在 AA 中导入人物时登记的真实 ID。
- 人物显示名称只写角色名；服装差分放在骨骼变体中，不写进人物名称。
- 如果电脑中有 Spine 3.8，程序可以渲染表情预览并使用多模态模型做语义标注。

## 程序要求补充背景时

看到“生成已暂停，不是失败”，说明现有素材库里没有足够准确的背景：

1. 复制页面给出的中文背景生成提示词。
2. 用图像模型生成横向 16:9 的纯背景图。
3. 点击“导入生成好的图片”，完成检查和导入。
4. 也可以点击“选择已登记背景”，手动指定一个合适背景。
5. 所有背景处理完成后，点击“继续生成 AAP”。

继续生成不会重新调用剧本标注模型。

## 模型设置

点击左下角“设置”：

1. 新建或选择模型配置。
2. 填写接口类型、接口地址、模型名称和 API Key。
3. 可勾选“保存到 Windows 凭据管理器”。密钥不会写入普通 JSON 文件。
4. 先点击“测试文字”，需要识别表情时再点击“测试图片”。
5. 测试成功后点击“设为当前”。

不要把 API Key 发给其他人，也不要把它写进接手文档、截图或发布包。

## 打不开时怎么办

双击总目录里的 `检查运行环境.cmd`。窗口会逐项显示：

- Python
- 程序文件
- 素材数据库
- 图片组件
- AA 工作区

如果启动失败，详细结果还会保存在：

`01-完整程序\aa\启动失败日志.txt`

## 两个常用入口

- 正常使用：`启动AA自动写剧本.cmd`
- 出现问题：`检查运行环境.cmd`

程序页面左下角还有“使用帮助”，可随时重新查看相同步骤。
