# HaloCue：从这里开始

这份说明写给普通 Windows 用户。使用公开 Windows ZIP 时，不需要命令行，也不需要安装 Python。

## 1. 第一次启动

1. 下载 `HaloCue-0.9.3-windows-x64.zip`。
2. 右键“全部解压”，进入解压后的 `HaloCue` 文件夹。
3. 双击 `HaloCue.exe`，HaloCue 会直接打开独立应用窗口，不调用系统浏览器。
4. 选择你自己安装的 `AzureArchive.exe`。
5. HaloCue 会读取 AA 自己的设置，自动识别项目与存档位置；不需要手工选择 AA 的存储文件。

不要直接在压缩包预览窗口中运行 EXE。Windows 首次提示网络访问时，HaloCue 只需要本机回环连接；不需要开放公网访问。

## 2. 做出第一章

1. 导入 `.txt` 或 `.md` 剧本。
2. 确认角色映射：旁白选“旁白”，不需要立绘的角色选“无立绘”。
3. 检查开场背景、缺失素材和诊断信息。
4. 选择“AI 自动安排演出”或“仅转换格式”。
5. 在审查页确认结果，再点击编译；需要时安装到自己的 AA 工作区。

推荐剧本格式：

```text
## 场景一：商店街，午后

旁白: 商店街人声嘈杂，凯伊已经在服装店门口等候。
老师: 久等了。
凯伊: ……你为什么要把「普通」说得那么不普通。
```

推荐一行一个角色，并在旁白中写清真实动作和位置变化。标点应表达实际语气，
不要为了触发演出而机械堆叠省略号或感叹号。程序不会修改原文；AI 模式只添加
演出标注，最终仍由你在审查草稿中确认。

## 3. Spine 与个人素材

公开版不包含 Spine，也不包含任何游戏资源或个人骨骼。找不到 Spine 时，普通剧本导入和编译仍可继续；只有骨骼预览、表情分析等相关功能不可用。

如需这些功能，请在设置中明确选择你自己合法安装的 Spine 命令行程序。HaloCue 不会自动扫描其他磁盘，也不会把 Spine、个人骨骼、图集、纹理或作品上传出去。

个人骨骼和个人素材在公开版、公开源码、私发覆盖包中都不得包含。

## 4. 配置、备份和迁移

个人配置、数据库、草稿、缓存和输出位于：

```text
%LOCALAPPDATA%\HaloCue
```

把 HaloCue 解压到另一台电脑后，先正常启动，再在设置中选择那台电脑上的 AA 路径。不要复制旧电脑的绝对路径配置来覆盖新配置。

备份方法：退出 HaloCue，复制整个 `%LOCALAPPDATA%\HaloCue` 文件夹。

恢复方法：退出 HaloCue，用备份文件夹替换当前文件夹前，先把当前文件夹重命名保留。

## 5. 常见问题

### 找不到 AA 工作区

重新选择真实的 `AzureArchive.exe`。HaloCue 会通过 AA 内部设置判断工作区；不要选择 HaloCue 的解压目录，也不要手工选择某个存档文件。

### 找不到 Spine

这是公开版的正常状态。只做剧本和 AA 工程时可以忽略。确需骨骼预览时，在设置里手动选择你有权使用的 Spine 程序；不要从他人发布包中获取 Spine。

### 端口冲突

先关闭其他 HaloCue 窗口，再重新启动。高级用户可用
`HaloCue.exe --port 8766` 选择另一个本机端口。

### 干净地重新配置

1. 退出 HaloCue，并确认任务管理器中没有 `HaloCue.exe`。
2. 打开 `%LOCALAPPDATA%`。
3. 把 `HaloCue` 文件夹重命名为 `HaloCue-backup`，不要直接删除。
4. 再启动 HaloCue，重新选择这台电脑上的 AA 路径。

这样可以随时改回旧文件夹，不会立即丢失草稿和设置。

### 应用窗口没有打开

先退出其他 HaloCue 实例，再重新双击 `HaloCue.exe`。如果提示缺少 WebView2，请安装 Microsoft Edge WebView2 Runtime 后重试；程序不会回退到外部浏览器。

### 模型连接失败

“仅转换格式”无需模型。使用 AI 时检查接口地址、模型名和 API Key，并先点击设置页的连接测试。不要把 Key 写进剧本、配置截图或求助信息。

## 6. 版本与来源

当前版本是 **HaloCue 0.9.3**。公开包名为
`HaloCue-0.9.3-windows-x64.zip`；私发包固定命名为
`HaloCue-0.9.3-private-windows-x64.zip`，不上传 GitHub，只有在具备相应授权时才能定向制作。
