# 更新记录

## 0.95 — 2026-08-23

- 统一桌面发布线为 0.95，公开包和私发包命名同步切换到 `HaloCue-0.95-*`。
- 纳入 0.95 线的 AI 演出流程、角色表情白名单、人物别名和背景分类/搜索能力。
- 公开发布链路改为 0.95 正式版校验、构建和 GitHub Release 说明。
- 0.9.3 保留为历史兼容基线，不再作为当前正式版本展示。

## 0.9.3 — 2026-08-13

- 修复部分干净 Windows 环境中 `pywin32` 凭据组件加载不完整时，模型配置错误拖垮整个设置状态接口的问题。
- Windows 凭据接口不可用时自动切换到系统 `ctypes` 实现；模型密钥故障不再影响 AA 工作区和 Spine 检测。
- 设置页新增“复制运行环境诊断”，只报告可用性，不包含本机路径、API Key 或其他敏感数据。
- 补齐桌面运行和打包依赖：`pywebview`、`pythonnet`、`clr_loader`、`pywin32` 与 PyInstaller。
- 公开 Release 仍不包含 Spine、游戏资源、个人配置、作品或个人素材。

## 0.9.2 — 2026-08-13

- Windows PC 版改为独立 WebView2 应用窗口，不再打开系统浏览器。
- 发布一目录 x64 便携包，解压后运行 `HaloCue.exe`，无需安装 Python。
- 首次使用改为在应用内选择 `AzureArchive.exe`，再读取 AA 自身设置自动判断项目和存档位置。
- “全部标记已审”使用应用内确认框，修复嵌入式窗口中的取消、焦点和重复提交问题。
- 完成长篇、多人物剧情的状态化导演、分块续跑、素材核对和审查流程更新。
- 公开包继续排除 Spine、游戏资源、用户配置、作品与个人素材。
- 私发覆盖包的版本名统一为 `HaloCue-0.9.2-private-windows-x64.zip`，且不上传 GitHub。

## 0.9.0-beta.1 — 2026-08-09

首个计划公开发布的 **HaloCue 0.9 Beta**。

- 提供本地网页式剧本导入、演员确认、审查和 AA 工程编译流程。
- Windows 公开包 `HaloCue-0.9.0-beta.1-windows.zip` 可直接运行，不需要安装 Python。
- 源码支持 Python 3.10–3.13。
- 配置、草稿、数据库和输出迁移到 `%LOCALAPPDATA%\HaloCue`，可在不同电脑上重新选择 AA 路径。
- 加入脱敏公开标签数据库、确定性公开源码导出、隐私扫描和可复现 Windows 打包。
- 加入 HaloCue 图标、版本信息和依赖许可清单。
- 加入干净 Windows 首次启动、重启、导入、审查、编译及退出验证。
- 公开版不包含 Spine，也不包含游戏资源、个人骨骼或个人作品。
- 预留 `HaloCue-0.9.0-beta.1-private-windows.zip` 私发覆盖包流程；没有 Spine 再分发书面授权时不会制作该包。
