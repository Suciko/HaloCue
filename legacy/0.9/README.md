# HaloCue 0.9 compatibility boundary

The maintained 0.9 implementation currently lives at the repository root. This
directory documents the future migration boundary; it intentionally contains no
duplicate source. Move a module here only as part of a tested migration issue.
