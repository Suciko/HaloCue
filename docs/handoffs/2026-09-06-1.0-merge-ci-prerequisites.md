# 1.0 stacked-PR integration: CI prerequisites

- Kind: handoff
- Observed: 2026-09-06
- Scope: CI/test infrastructure only; refs #20, integration of #33 / #35 / #37
- Branch: `codex/1.0-ci-prerequisites`
- Base: `feature/1.0-adaptation-workflow` at `3189300c83c8291bc19e9ab52ac66d5bf8d4e33e`
- Governing records: product direction, backend context and remote collaboration protocol.

## Evidence and fix

Remote base run `33961475617` and Sel run `34007049169` share missing-ffprobe
failures, an outdated release-workflow assertion and a timezone-specific recent
story assertion. No new failure identities occur in the Sel run relative to
that base. This is a failure-identity comparison, not a native-AA acceptance.

- Install pinned Chocolatey `ffmpeg` **9.0.1** before tests and release validation
  in both workflows. Print the discovered ffprobe path/version and fail on setup
  errors. No application validation, unavailable behavior or binaries change.
- Match the existing 1.0 PowerShell argument-splat publisher, signed update
  manifest and dynamic versioned archives, rather than obsolete 0.9 draft-release
  command spelling. Publication remains manual; this change does not publish.
- Exercise recent-story rendering with explicit UTC and Asia/Shanghai Node
  subprocess timezones. The app continues to display the user's local time.

## Local validation

Windows Python 3.12 in an isolated virtual environment; real ffprobe on this
machine; fixtures and isolated test user data only.

- Original release-workflow assertion reproduced before modification.
- Explicit UTC reproduces the original `09:30` versus `17:30` assertion.
- New installer-order assertions before workflow fix: **3 failed, 44 passed**.
- After fix:

```text
python -m pytest -q tests/test_release_workflows.py tests/test_ui_story_workspace.py tests/test_asset_validation.py tests/test_aa_registry.py tests/test_dual_registration.py tests/test_final_fix_wave.py tests/test_generator_asset_integration.py tests/test_history_assets.py tests/test_project_asset_integration.py services/halocue/production/tests/test_custom_asset_library.py
```

Result: **166 passed, 2 skipped**, 16.34 seconds. The skipped cases require
additional optional facilities; no failures were suppressed. `git diff --check`
passes. Hosted Chocolatey installation and the three hosted Python matrix runs
still need their actual CI results; local checks do not substitute for them.

## Integration order and remaining acceptance

Merge this isolated prerequisite PR, then #33, #35 and #37 into
`feature/1.0-adaptation-workflow`, preserving commits. Retarget the stacked PRs
as each predecessor lands. Do not merge to main, modify 1.1 or publish a release.
Record final merge commits and full-candidate validation in the PR discussion.

Native AA acceptance remains outstanding: slot-zero teacher dialogue, single
Sel, consecutive Sel, terminal Sel to Exit, five visible portraits, and CG.
Only after those pass should multi-branch Sel state/edges/save/restore/backtracking
be designed. No DOCX/XLSX importer is added in this integration.
