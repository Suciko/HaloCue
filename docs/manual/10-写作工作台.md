# 写作工作台

## 作品、章节和场景

作品是最大的范围。卷和章用来整理结构，场景是一次写作和审查的最小单位。Agent 只读取当前场景以及你明确加入的资料。

## 三个阶段

- 细纲：整理章节顺序、场景目标和上下文。
- 正文：写对白、旁白和动作描述。保存后会产生新的 Revision。
- 发布：检查连续性和演出安排，确认后生成不可变的 ScriptRelease。

## 修改正文

模型生成的内容先进入 Proposal。打开 Diff，确认改动后再点“采纳”。不满意可以拒绝，不会影响当前正文。多个窗口同时修改时，如果版本不一致，系统会提示冲突，不会替你覆盖别人的修改。
