# 编译核心来源

本目录中的 `script2aap.py`、资源表和依赖模块于 2026-08-10 从仓库的 `01-完整程序/aa/` 同步，用于让 Chaquopy 在 APK 内复用同一套 AA 工程结构生成逻辑。

- 同步入口：`06-安卓端/scripts/sync-compiler-core.ps1`；脚本可从主工作区或任一 Git worktree 执行。
- 安卓专属入口：`android_compiler.py`
- `spine_semantic_faces.py` 同步为只读的 Spine 3.8 表情组合解析器。它只解析 `.skel` 中的编号动画、插槽和附件名称，不包含渲染运行时或 Windows 工具依赖。
- 桌面源 `script2aap.py` SHA-256：`FFB0539008FC3EFC72581D4E0B1F9A40AAC4AF0D083B8DCFCEFE099074F4CC4A`
- 桌面源 `aa_resources.json` SHA-256：`522C77513802BCD624AA25182511DAF9B37B73A0BBA929269D1669CE00F21DF6`
- 同步后唯一的兼容性改动：调用 `sys.stdout.reconfigure` 前先检查该方法是否存在。

更新桌面编译器后，先运行同步脚本，再运行 `app/src/test/python/test_android_compiler.py` 和真机仪器测试。不要直接把 Windows 的安装路径或 AA 目录探测逻辑当作安卓写入能力。
