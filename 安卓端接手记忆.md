# HaloCue 安卓端接手记忆

更新时间：2026-08-10

## 一句话结论

安卓端方案已经确定：保留原版 AzureArchive（AA）不变，单独制作一个可独立运行的 HaloCue APK。APK 使用原生 Android 外壳承载现有窄屏 WebUI，并在手机本地运行移植后的 Python 编译后端；生成 `.aap/.aas` 后，稳定模式通过公共目录交给文件管理器，vivo 设备再提供需要用户一次性授权的实验性自动导入。

## 用户要求

- 用户安装的必须仍是原版 AA，不能要求安装修改版 AA。
- HaloCue 必须是独立 APK，不依赖电脑常驻，也不是跳转到普通网页。
- 界面应像正常安卓应用：应用内 WebView、无浏览器地址栏、可调用系统文件选择与原版 AA。
- 用户接受一次性授权；目标用户通常已经使用过安卓端 AA。
- 安卓端代码、文档和测试产物必须与电脑端分开，统一放在 `06-安卓端/`。

## 已确认的 AA 信息

- 分析样本：仓库根目录 `AzureArchive_AND_0.8.7.apk`
- 包名：`com.foxxlight.AzureArchive`
- 版本：`0.8.7`
- `minSdkVersion`：23
- `targetSdkVersion`：30
- 技术栈：Unity IL2CPP
- 项目目录：`/storage/emulated/0/Android/data/com.foxxlight.AzureArchive/files/data/projects/`
- 存档目录：`/storage/emulated/0/Android/data/com.foxxlight.AzureArchive/files/data/saves/`
- AA 只扫描上述目录内的 `*.aap` 和 `*.aas`。
- AA 没有提供接收 `.aap` 的公开导入 Intent、ContentProvider 或 FileProvider。
- AA 的扩展包安装与项目导入是两套机制；安装扩展包后才可能出现 `projects`、`saves` 等目录。

## 真机验证结果

测试设备：vivo X100s Pro，型号 `V2324HA`，Android 16 / API 36，OriginOS 16。

已通过 USB 调试确认：

- 原版 AA 0.8.7 正常安装。
- AA 项目目录中存在原项目 `1.aap` 和目录 `1/`。
- Android 16 的系统目录授权界面无法选择 `Android/data`；目录被隐藏或“使用此文件夹”按钮不可用。
- `MANAGE_EXTERNAL_STORAGE` 也不能让普通 APK 写入其他应用的 `Android/data` 专属目录。
- 将临时 `.aap` 分享给 vivo 文件管理器时，文件会被自动复制到 `Download/分享保存/`，不能通过公开参数指定 AA 目录。
- vivo 文件管理器存在 `com.vivo.filemanager.intent.action.OPEN_APP_FILE_LIST` 内部入口，但它要求 `com.vivo.filemanager.permission.OPEN_FILE_LIST`，保护级别为 `signature|privileged`，普通 APK 无法获得。
- AA 不是 debuggable 包，不能使用 `run-as`。
- 测试产生的两个临时 `.aap` 副本已经删除；AA 原项目未修改。

### 2026-08-10：首个独立 APK 技术探针已通过

- 已建立标准 Android Gradle 工程，包名 `com.halocue.android`，版本 `0.1.0-dev`。
- 已将本地 HTML/CSS 资源打进 APK，由应用内 WebView 显示；主界面不跳转浏览器。
- 已接入 Chaquopy 17.0.0 和 Python 3.13，真机页面显示“本地 Python 已启动”。
- 已通过 Android `PackageManager` 检测到原版 AA 0.8.7，包名仍为 `com.foxxlight.AzureArchive`。
- 已从 HaloCue 点击“打开原版 AA”，在 vivo 的应用间跳转确认中选择“仅打开一次”后，前台成功进入 `com.foxxlight.AzureArchive/com.unity3d.player.UnityPlayerActivity`。
- Android 真机仪器测试通过：vivo `V2324HA` / Android 16 上执行 1 个测试，0 失败，Gradle 输出 `BUILD SUCCESSFUL`。
- Python 合同测试通过：1 个测试，0 失败。
- 可安装调试包：`构建产物/HaloCue-Android-MVP-0.1.0-dev-debug.apk`（SHA-256：`F157C2FD2CCC355DC6BDF0AC94AACA0CE764DD009AEBFC5E74F13F3FA7603487`）。
- 真机截图：`evidence/halocue-mvp-vivo-x100s-pro.png`。
- 本次 APK 只申请 `android.permission.INTERNET`；未申请存储权限，未读写 AA 项目目录。
- vivo 安装注意：手机锁屏时，ADB 安装会以 `INSTALL_FAILED_ABORTED: User rejected permissions` 失败；解锁并亮屏后可继续。外部来源拦截页需要点“授权本次安装”，不必长期关闭“超级守护”。
- Windows 中文工程路径会让 Android 测试工具中的旧版 `aapt` 出现乱码；真机测试时将工程临时映射到纯英文盘符即可，不需要移动源码。

### 2026-08-10：0.2 本地 `.aap` 编译闭环已通过

- 当前版本为 `0.2.0-dev`。APK 已内置从电脑端同步的实际 `script2aap` 编译核心，不是伪造文件或页面演示。
- 同步入口为 `scripts/sync-compiler-core.ps1`，来源、文件清单与哈希记录在 `app/src/main/python/编译核心来源.md`；Android 适配入口为 `app/src/main/python/android_compiler.py`。
- Chaquopy 继续使用 Python 3.13，并加入 Android arm64 可用的 Pillow 11.0.0。
- Kotlin 桥 `AndroidCompilerBridge.kt` 把 `context.filesDir/compiler` 作为唯一可写工作区；页面编译在后台线程运行。
- 主机 Python 合同测试通过：2 个测试，0 失败。vivo 真机上的编译桥仪器测试通过：1 个测试，0 失败。
- 在 vivo X100s Pro / Android 16 上从应用页面实际生成 `AndroidDemo.aap`，含 1 条台词；界面明确显示“已生成，尚未导入 AA”。
- 真机文件位于 `/data/data/com.halocue.android/files/compiler/exports/AndroidDemo.aap`，大小 4469 字节，SHA-256 为 `a3b5d2d1da15d6201323fd8195fdb37f126be8b5dd8c6e9ffa0aa05844936dd6`。
- 截图证据为 `evidence/halocue-v020-generated-vivo-x100s-pro.png`。
- 可安装调试包为 `构建产物/HaloCue-Android-MVP-0.2.0-dev-debug.apk`（23,135,419 字节；SHA-256：`64F0243B72FE097C91B30F131A5AE91382B7A17046FD031A389CA6E1D574280D`）。
- 仍未申请存储权限，也没有读取、覆盖或删除原版 AA 的任何工程。
- vivo 的 WebView/Espresso 页面测试偶尔会长期停在测试框架内部；页面结构由 UI hierarchy 验证，真实生成链路由页面操作、真机编译桥测试及私有文件校验三重确认。不要把这类框架卡顿误判为编译耗时。

## 已否决或不作为默认方案的路线

### 直接申请文件夹权限

不可行。Android 11 及以上禁止普通应用通过系统文件选择器取得 `Android/data` 及其子目录授权。

### “所有文件访问”权限

不可行。该权限仍不能访问其他应用在 `Android/data` 下的专属目录。

### 降低 targetSdk 或使用旧存储模型

不可行。Android 11 起，对其他应用外部专属目录的限制不取决于本应用的 targetSdk。

### Shizuku 作为大众默认方案

不采用。Android 16 已有多个文件管理器经 Shizuku 仍无法写入 `Android/data` 的报告，而且服务可能需要重启后重新启动。

### 调用 vivo 私有 Intent

不可行。相关入口需要 vivo 系统签名权限。

### 修改或重签 AA

禁止。违反“用户继续安装原版 AA”的硬要求。

## 确定的技术架构

### 1. Android 外壳

- Kotlin 原生应用。
- 单 Activity 或少量 Activity。
- 应用内 WebView 加载随 APK 打包的前端资源。
- 不打开 Chrome，不显示网址，不依赖远程网页作为主界面。
- 外部链接才交给系统浏览器。

### 2. Python 后端复用

- 首选 Chaquopy，将现有 Python 核心打包进标准 Android Gradle 工程。
- 保留脚本解析、LLM 请求、`.aap` 生成、校验、草稿和数据库逻辑。
- 不直接照搬电脑端启动器；Android 端提供独立入口和适配层。
- 可继续使用本地 HTTP API + WebView，也可逐步改为 JS/native/Python 桥接；第一版优先复用现有 HTTP API，减少前端改动。

### 3. 必须替换的电脑专属逻辑

- `AzureArchive.exe` 和 Windows AA 安装目录发现。
- `AppData/LocalLow` 路径。
- tkinter 文件选择器。
- Windows Credential Manager / `win32cred` / `ctypes.WinDLL`。
- 依赖桌面浏览器启动和 Windows 进程检测的逻辑。
- 对 AA 工作目录直接读写的安装流程。

### 4. Android 数据策略

- HaloCue 的数据库、草稿、缓存和生成中间产物放在自身应用目录。
- 剧本、扩展包和自定义素材通过 Android 系统文件选择器由用户选择。
- 官方资源不随 APK 分发。
- 如需要建立素材预览，优先让用户一次性选择自己持有的 AA 扩展包压缩文件，HaloCue 在自身目录内建立索引。
- API Key 使用 Android Keystore 加密保存，不沿用 Windows 凭据管理器。

### 5. 输出与 AA 联动

第一版同时保留两条路径：

1. 稳定路径：生成到 HaloCue 自身目录，再导出到 `Download/HaloCue/`，随后打开系统或厂商文件管理器，明确提示用户复制到 AA 的 `projects` 或 `saves`。
2. vivo 实验路径：用户主动开启辅助功能后，由 HaloCue 在用户点击“导入到 AA”时操作 vivo 文件管理器完成复制，再启动原版 AA。

辅助功能只能作为透明、用户主动触发的可选功能，不能静默运行，也不能伪装系统提示。它会受到 vivo 文件管理器界面更新影响，必须有失败检测和随时退回稳定路径的能力。

## 建议目录结构

```text
06-安卓端/
  安卓端接手记忆.md
  README.md
  docs/
    安卓端设计规格.md
    真机验证记录.md
  app/                    # Android Gradle application module
  python/                 # 从电脑端移植/共享的 Python 适配入口
  tests/
  evidence/               # 截图、UI dump、兼容性记录；不放用户隐私数据
```

实际建立工程时以 Android Studio/Gradle 的标准目录为准；不要复制整套电脑端代码后独立分叉。应抽取或同步可复用的纯 Python 模块，Android 专属修改放在适配层。

## 推荐实施顺序

1. 建立最小 Android 工程，WebView 本地加载现有窄屏 UI。
2. 接入 Chaquopy，验证最小 Python 函数可被 Kotlin 调用。
3. 移植不依赖 Windows 和 AA 文件系统的 `.aap` 生成最小链路。
4. 在 APK 内完成“输入文本 → 生成 `.aap` → 校验 → 导出到 Download”的真机闭环。
5. 适配模型配置、Android Keystore、草稿和素材数据库。
6. 实现扩展包选择与本地索引。
7. 实现启动原版 AA 和稳定手动导入引导。
8. 最后单独开发 vivo 辅助功能自动导入，不让它阻塞基础 APK 发布。
9. 再决定是否扩展到小米、OPPO、荣耀、三星等品牌。

## 第一阶段完成标准

- APK 能在 vivo X100s Pro / Android 16 独立启动。
- 主界面在应用内部显示，返回键、文件选择、键盘和安全区域正常。
- 不连接电脑也能完成一次最小 `.aap` 生成与结构校验。
- 生成文件能导出到公共目录。
- 能检测原版 AA 是否安装并启动它。
- 失败时有明确提示，不能误报“已经导入 AA”。
- 不读取、覆盖或删除 AA 原项目。

## 尚未完成

- 尚未验证 Chaquopy 与电脑端全部 Python 模块和第三方包的兼容性；当前只验证了最小 `.aap` 编译依赖集。
- 尚未把电脑端完整 WebUI 和模型配置迁入 APK；当前 APK 已能真实生成最小 `.aap`，但仍不是功能完整版。
- 尚未实现生成文件导出到 `Download/HaloCue/` 的闭环。
- 尚未验证辅助功能能否稳定自动操作当前版本的 vivo 文件管理器。
- 尚未决定安卓端首版包含哪些电脑端高级素材功能。

## 接手 AI 的第一步

先阅读本文件，再检查电脑端以下入口，不要重新猜测 AA 存储路径：

- `01-完整程序/aa/webui.py`
- `01-完整程序/aa/ui.html`
- `01-完整程序/aa/script2aap.py`
- `01-完整程序/aa/aapaths.py`
- `01-完整程序/aa/model_profiles.py`
- `01-完整程序/aa/story_file_picker.py`

“输入文本 → 实际 `script2aap` 编译 → 生成 `.aap` → 真机文件校验”已经完成。下一步先实现“导出到 `Download/HaloCue/`”，确保不连接电脑也能拿到生成文件；随后再迁移完整窄屏 WebUI。不要直接写入 AA 目录。

## 重要原则

- 原版 AA 不修改、不重签、不注入。
- 不能把 ADB shell 的能力误认为普通 APK 的能力。
- 不能声称普通文件权限能突破 `Android/data`。
- 任何自动导入都必须在用户主动触发、明确授权和可见界面下执行。
- 自动导入失败必须保留生成文件，并允许用户走稳定手动路径。
- 每次真机测试使用唯一的临时文件名，测试后只清理本次创建的文件。
