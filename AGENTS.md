# 0.9x AA 演出后端持久化交接规则

本文件是继续 0.9x 盲测迭代时的首要交接入口。先读本文件，再读
`docs/0.9x-blind-iteration-causal-strategy.md` 和当前输出目录中的真实状态。

## 压缩恢复短规则

上下文压缩或 Agent 交接后，先恢复下面这条工作判断，再读真实文件：

`新文本不是官方事件链复现 -> 先理解上文刺激和关系因果 -> 再决定承受者、表演资源、镜头时机和跨节点状态`

- 官方文本、AAP、人工标注和旧模型结果只能在生成后做对照，不能变成盲测答案模板。
- 先看真实原始 AI JSON 和链路审计，再看最终 AAP；内部质量分不能代替官方差异。
- 三个场景必须同一轮、同一版本号分别盲测；只重试失败 request，保留全部 attempt。
- 发现新版本退化时，不能停在汇报或等待用户决策：定位到提示词/模型、协议、策略或编译层，修复后继续跑到有真实 AAP 和可人工审查差异报告。
- 不得把编译成功、字段完整或质量分上升说成“接近官方”或“75%”；没有可复现的官方相似度计算器就明确说明未知。

## 核心判断模型

导演行为只有两个源码入口：

- G1 事件与因果规划：`annotation_scene_planner.py::PLANNER_SYSTEM_COMPACT`
- G2 具体演出选择：`prompt.py::DIRECTOR_DECISION_RULE`

修改“积极演出、最低工作量偏置、关系镜头、表演强度或静默拍”时，先读这两个入口，并只在对应阶段的权威
提示词中修改；本文件只保存迭代边界，不复制审美条款。两阶段都用 `READABLE CHANGE` 指代同一个目标：
有因果意义的变化必须让观众可读，保持则必须是有意的表演或真实连续。

不要把官方节点顺序当作新文本的答案模板。审查每个对白或无对白节点时使用：

`上文刺激 -> 承受者/说话人 -> 情绪或身体反应 -> face/emo/act/bubble -> 镜头时机 -> 下一节点继承状态`

提示词和模型事件理解是演出质量的主要来源。出现“长期同脸、emo/act 归零、应有的情绪拍消失”时，先检查原始 G1/G2
是否逐节点比较了状态，不先增加后端审美硬规则。后端只保存并提供 `last_performance_node`、逐角色最后表情和空间状态，
不替 AI 决定换脸、气泡、动作、镜头或静默节点。

官方资料只在生成完成后用于差异对照、视频研究和提示词研究；盲测生成不得读取官方答案、旧 AAP、人工标注、官方解析或旧模型结果作为答案参考。可以学习官方的因果、关系镜头、静默信息和表演语法，但不能复制事件链。

## 三层审查顺序

1. 文本因果：反应是否由上文触发，承受者是否合理，跨 chunk 的物理存在、镜头可见人物和槽位是否连续。
2. 表演因果：face、emo、act、bubble 是否符合语义和标点，表演阶段是否真正演进；不同意图不得被压成同一个 face-only。
3. 镜头因果：镜头表达互动关系、信息揭晓或情绪峰值；说话人不必与对方同框，单人特写和硬切是合法语言；禁止无解释瞬移和不完整 reframe。

静默节点只在对白无法独立展示的新信息存在时使用，例如入场被看见、发现、操作反馈、关系反应、情绪余波或决定停顿。无对白节点不等于 Wait 配额；有台词节点不得额外生成 Wait。

无对白能力边界：不要把连续多节点、完整默剧式的长段无对白演出当作 AI 的默认职责、质量门或盲测硬指标。AI 只需在一个关键反应、操作结果或决定必须先于下一句被观众看见时，主动设计短促且因果明确的独立 beat；能够自然设计更长的无对白链可以保留，但缺少长默剧本身不判失败，也不得为了接近官方节点数量强迫补齐。复杂连续动作优先由对白节点承载可读里程碑，或留给后续人工导演，不要求模型完整复刻官方的长无对白编排。

## 后端边界

后端只负责协议解析、资源合法性、身份/装束、状态生命周期、槽位和几何安全、单镜最多三人、格式修复及可证明的重复指令去重。不得替 AI 做审美、节奏、镜头意图、表情阶段或静默数量决策，也不得静默删除 AI 明确字段。任何字段从原始 JSON 消失，都必须记录发生在协议展开、校验、策略或渲染编译哪一层，以及具体原因。

硬切次数不设上限。`move/reveal/conceal/enter/exit` 必须更新跨 chunk 的物理状态、可见人物和槽位；`conceal` 只移除当前镜头中的立绘，不改变人物仍在场的状态。镜头去重必须考虑显式 camera、上述生命周期指令、camera reset 和场景边界；只有可证明的重复声明才允许去重。

## 盲测轮次规则

- P03、圣娅 Code:BOX、3-1-7 必须同一轮使用同一个版本号，例如都为 V6；三段仍使用独立目录、`source_id`、请求/响应、checkpoint fingerprint 和审计文件。
- 同一轮只重试失败的请求；保留所有原始响应和 attempt，不覆盖旧盲测封存。网络失败不能阻塞其他独立场景。
- 每个 Agent chunk 必须保留：原始 AI JSON、compact 展开、validate 结果、后端策略结果、最终 AAP trace 和 `.model-audit.json`。
- 不设置硬字符预算，不覆盖模型配置的最大输出 token。三阶段场景计划在现有测试和盲测证明前不得当作正式默认。
- 运行工具失败时，只报告真实生成的文件；不得把未运行或未编译的结果说成完成。禁止使用无意义的命令 `Write-Output 'tool namespace unavailable from nested exec'`。

## 退化诊断

- 原始 AI 已缺少或误解因果：改提示词/场景计划或重新调用模型，并明确标记为 AI 新生成。
- 原始 AI 正确但 compact/validate 丢失：修协议/校验，并标记为后端修复。
- 原始 AI 通过但 AAP 缺字段或状态断裂：修策略/编译器，并保留前后审计。
- 内部质量分上升但与官方差异没有缩小，或视觉上变差：不保留该改动，优先回查是否硬限制过多、关系镜头被压扁、静默信息被删除或表演阶段被去重。

## 持续迭代要求

- 每个新编译的 AAP 都必须和对应官方命令流、视频解析及最新人工标注做逐段差异评估；不能只看内部 quality 分，也不能把旧基线当作新成绩。
- 若官方差异变大、视觉连续性退化或有效表演被压缩，立即回查原始 AI JSON -> compact -> validate -> policy -> AAP trace，定位是提示词/模型、协议、策略还是渲染编译问题，并继续修复或重新调用失败场景。
- 三场景同一轮完成后才决定是否保留提示词或后端改动；不得因为单独改善圣娅就推断主线泛化成功。
- 在没有形成可人工审查的真实 AAP、审计链和官方差异报告前，持续执行检查、修复、编译、评分循环；只有遇到真实外部阻塞才向用户报告并说明已生成与未生成的文件。

## 接续步骤

1. 先检查 `git diff`、输出目录、checkpoint、请求和响应文件及当前测试状态。
2. 阅读本文件和因果策略文档，再读取当前失败场景的真实 `resume-needed`/repair 请求。
3. 只消费失败场景，逐字段核对 raw -> compact -> validate -> policy -> AAP trace。
4. 三段同轮完成后，查看 AAP 实际脚本和官方逐段差异，再决定是否保留提示词或后端改动。

## 发布日志口径

更新日志面向使用者，保持专业但易读。先写相对上一版真正新增的能力，再写已经确认修复的用户可见问题；不要把 0.945 已有的桌面 EXE、WebView2、内置 Python/第三方库或基础打包能力重复包装成 0.95 新功能。发布构建、自检、路径扫描和数据库清理属于内部交付工作，应放在维护说明或验证记录中，不应伪装成用户输入错误或版本功能修复。每条“修复”都必须能指向旧版本的真实问题和对应代码/测试证据；不确定时写成限制或待验证项，不要虚构改进。
